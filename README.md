Import X4 constructionplan XML to http://x4-game.com

### Usage
`./x4gci.py <path>` where `<path>` is the path to the XML file containing the construction plan.

#### Example
```bash
./x4gci.py samples/mega\ 2.xml
```